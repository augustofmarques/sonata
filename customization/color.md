# Customization — Color

Users may receive:

1. curated Sonata-safe accent choices;
2. an advanced custom color input.

A source color is transformed into a usable semantic palette rather than directly replacing every component color.

The application may restrict choices, provide its own palette or allow arbitrary input.
